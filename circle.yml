machine:
  services:
    - docker

test:
  override:
      - docker info
      - docker build -t coinpit/firewall .
      - docker login -u $DOCKER_USER -p $DOCKER_PASS
      - docker push coinpit/firewall

